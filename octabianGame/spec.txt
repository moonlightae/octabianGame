//1. 문제 개요
//문어외계인군단이 나타났다. 우주선을 왼쪽, 오른쪽으로 조정하면서 총알을 쏴서 문어외계인을
//격추시키는 게임을 만든다.

//2. 구성 요소
//1) 우주선(spaceship.bmp)

//플레이어는 우주선을 조정한다. 초기 우주선은 중앙아래에 위치해 있다. 이 때 우주선은 왼쪽, 오른쪽으로만 움직일 수 있고, 화면 밖을 벗어날 수 없다.
3)의 총알을 쏠 수 있다. 문어외계인 군단이 닿으면(어디에?) 목숨이 없어지고 게임은 다시 시작된다.<< 맞을때마다 재시작. 목숨의 개수는 3개이며, 목숨의 개수를 화면의 왼쪽 상단에 표시한다.
//2) 적(octopus.bmp)

//플레이어가 제거해야 할 적이다. 문어외계인군단은 왼쪽 상단부터 시작하여 문어외계인군단은
//좌우로 움직이며 경계에 도달할 때마다 앞으로 전진한다. 총알에 맞은 문어외계인은 제거된다.

//3) 총알(bullet)
//총알은 너비가 3픽셀, 높이가 15픽셀이며, RGB 값은 (60, 60, 60)이다. 총알의 속도는 2다. 총
//알은 플레이어가 space 키를 누를 때마다 발사된다.

4) 점수
//화면의 오른쪽 상단에 점수가 표기된다. 외계인을 맞출 때마다 10점씩 증가한다.

//3. 코드 설명
//1) GamePro 클래스
//게임 자원과 동작을 전체적으로 관리한다. 게임 화면의 제목표시줄은 “문어외계인 침공(이름)”
//으로 수정한다. 구분 속성 설명

//인스턴스
//변수

//clock

//pygame.time 모듈의 Clock 클래스 인스턴스
//run_game()에서 self.colck.tick()로 게임 프레임 속도를
//인수로 받아 시간 제어
//settings Settings 클래스
//screen 표시할 창 또는 화면을 초기화(너비, 높이 다 포함)

//(display.set_mode()활용)

//ship 우주선
//aliens sprite.Group()을 이용한 외계인 군단 생성
//bullets sprite.Group()을 이용한 총알그룹 생성

//메소드

//__init__(self) 생성자

//run_game(self)

//게임 실행
//check_events(), update_screen()이 실행되고, 시간 제어
//함수가 실행됨
//check_events(self) 이벤트가 발생하면 응답함


//2) Settings 클래스

//(닫기 버튼을 누르면 시스템이 종료된다거나 오른쪽 키를 누르면
오른쪽으로 움직이는 등)

//update_screen(self) 화면의 이미지를 업데이트하고 화면을 새로 그림

//create_fleet(self)

//문어 군단 생성
//화면 왼쪽에서부터 배치하여 공간이 있으면 문어를 계속 추가, 오
//른쪽 경계에 가려지지 않도록 배치함.(위 이미지 참고)

//change_direct(self)

//문어외계인 군단이 경계에 도달하면 한 줄 내리고 좌우 방
//향을 바꿈

//구분 속성 설명
//인스턴스
//변수

//screen_width 게임화면의 너비
//screen_height 게임화면의 높이
//bg_color 게임화면의 색깔
//spaceship_speed 우주선의 초기 속도
//bullet_color 총알의 초기 색상
//bullet_width 총알의 너비
//bullet_height 총알의 높이
//bullet_speed 총알의 초기 속도
//octopus_speed 문어외계인의 초기 속도

//메소드 __init__(self)

//초기 게임 화면은 가로 1200, 세로 600, 배경색은 RGB값
//(230, 230, 230)으로 한다.(*게임 화면의 크기는 적절하
//게 바꿔도 됩니다.)
//우주선의 초기속도는 1.5로 한다.

//3) Spaceship 클래스
//우주선 클래스.

//4) Bullet 클래스(Sprite상속)(Sprite는 pygame.sprite 모듈에 있음)
//구분 속성 설명

//인스턴스 변수

//screen GamePro의 인스턴스 변수 screen을 할당

//screen_rect
//get_rect()를 이용해 GamePro.screen의 rect속성 할당

//image spaceship.bmp 이미지 로드
//rect 이미지의 rect 속성
//self.rect.midbottom 우주선의 초기 위치를 화면의 하단 중앙으로 설정

메소드

//__init__(self, game)

//game: GamePro 클래스의 현재 인스턴스를 인수로 받
음

//draw_spaceship(self) 우주선을 현재 위치에 그림
update(self) 우주선 위치 업데이트

//구분 속성 설명

//인스턴스 변수

//screen GamePro의 인스턴스 변수 screen을 할당
//settings GamePro의 인스턴스 변수 settings를 할당
//color settings에서 초기화된 총알 색상을 불러옴
//rect 총알의 rect속성

//rect.midtop

//총알의 midtop속성을 우주선의 midtop 속성에 맞춰 우주선이 총알을 발사하는 것처럼 보임

//메소드

//__init__(self, game)

//game: GamePro 클래스의 현재 인스턴스를 인수로 받음
//우주선의 현재 위치에서 총알 객체를 만듦

//update(self) 총알이 나감

5

5) Octopus 클래스(Sprite 상속)
적 클래스.

4. 테스트하고 개선하기
아래의 기능을 추가하고 난 뒤 함수에 불필요한 부분이 있지는 않은지, 기능이 잘 분리되어 있
는지 등을 확인하고 수정하고 충분히 테스트한 뒤 제출해주세요.
1) 난이도 조정(우주선의 속도, 총알의 크기 등)
2) 총알 수 제한하기
3) 창을 벗어난 총알 제거하기

* 위에 제시되지 않은 부분은 자유롭게 구현할 수 있습니다.

5. AI 및 웹 검색 사용 안내

draw_bullet(self) 총알 표현

구분 속성 설명

인스턴스 변
수

screen GamePro의 인스턴스 변수 screen을 할당
settings GamePro의 인스턴스 변수 settings를 할당
image spaceship.bmp 이미지 로드
rect 이미지의 rect 속성
//rect.x rect의 너비 <<<< 이거 질문좀
rect.y rect의 높이

메소드

__init__(self, game)

game: GamePro 클래스의 현재 인스턴스를 인수로 받
음

update(self) 문어외계인을 오른쪽이나 왼쪽으로 움직임
check_edges(self) 문어외계인이 화면 경계에 도달하면 True 반환
